function res = aFlush(obj)
    % function res = obj.aFlush()
    % 
    % force all habitats to perform queued saves requested by aSave
    %
    % output
    % - res = (integer) 0: nothing to be done
    %                   1: performed operations
    %

    res = 0;

end %function
