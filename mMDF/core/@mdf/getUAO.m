function [uuid, object] = getUAO(indata)
    % function [uuid, object] = mdf.getUAO(indata);
    %
    % place-mark for function mdf.getUuidAndObject
    % please see help for mdf.getUuidAndObject
    
    [uuid, object] = mdf.getUuidAndObject(indata);
    
end %function
