function res = rul(source,dest,sProp)
    % function res = rul(source,dest,sProp)
    %
    % short cut to function rmUnidirectionalLink
    % check rmUnidirectionalLink help for more info
    %
    
    res = mdf.rmUnidirectionalLink(source,dest,sProp);
    
end %function