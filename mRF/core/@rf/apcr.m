function res = apcr(parent,child,prop)
    % function res = addpcr(parent,child,prop)
    %
    % short cut to function addParentChildRelation
    % check addParentChildRelation help for more info
    %
    
    res = rf.addParentChildRelation(parent,child,prop);
    
end %function