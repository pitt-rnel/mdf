function res = aul(source,dest,sProp)
    % function res = aul(source,dest,sProp)
    %
    % short cut to function addUnidirectionalLink
    % check addUnidirectionalLink help for more info
    %
    
    res = rf.addUnidirectionalLink(source,dest,sProp);
    
end %function